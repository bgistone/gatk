<project name="MedSciWalkers" default="compile" basedir=".">
       <description>
               Build file for MedSciWalkers
       </description>

       <!-- set global properties for this build -->
       <property name="src.main" location="src" />
       <!-- <property name="src.test" location="src/test" /> -->
       <!-- <property name="build" location="build"/> <property name="dist" location="dist"/> -->

       <target name="init">
               <!-- Create the time stamp -->
               <tstamp />
               <!-- Create the build directory structure used by compile -->
               <mkdir dir="${build.dir}" />
       </target>

       <target name="compile" depends="init">
               <javac srcdir="${src.main}" destdir="${build.dir}" classpath="${gatk.classpath}" />
       </target>

       <!-- <target name="test.java.external.compile" depends="init">
               <javac srcdir="${src.test}" destdir="${build.dir}" classpath="${gatk.classpath}" />
       </target> -->

       <target name="dist" depends="compile" description="generate the distribution">
               <!-- Create the distribution directory -->
               <mkdir dir="${dist.dir}/lib" />

               <!-- Put everything in ${build} into the MyProject-${DSTAMP}.jar file -->
               <jar jarfile="${dist.dir}/lib/MedSciWalkers-${DSTAMP}.jar" basedir="${build.dir}" />
       </target>

       <target name="clean" description="clean up">
               <!-- Delete the ${build} and ${dist} directory trees -->
               <delete dir="${build.dir}" />
               <delete dir="${dist.dir}" />
       </target>
</project>
